{
  "name": "a1d-agent-aws-deployment",
  "version": "1.0.0",
  "description": "AWS deployment configuration for A1D Agent using Pulumi",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "up": "pulumi up -y",
    "preview": "pulumi preview",
    "destroy": "pulumi destroy"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/awsx": "^2.0.0",
    "@pulumi/pulumi": "^3.0.0"
  }
}